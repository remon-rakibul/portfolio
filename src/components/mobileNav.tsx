import { CiMenuFries } from "react-icons/ci";

const MobileNav = () => {
  return (
    <div>
      <CiMenuFries className="cursor-pointer text-3xl font-bold text-teal-400" />
    </div>
  );
};

export default MobileNav;
